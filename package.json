cat > package.json << 'EOF'
{
  "name": "facebook-webhook-to-sheets",
  "version": "1.0.0",
  "description": "Receive Facebook webhook events and forward to Google Sheets",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "googleapis": "^148.0.0"
  }
}
EOF

# 6. JSON anahtarlarını repoda tutmamamız için .gitignore
cat > .gitignore << 'EOF'
# Service Account key files
*.json
EOF